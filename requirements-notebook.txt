# Python deps for jekyll-jupyter-notebook (calls `jupyter nbconvert`).
# Install once: pip install -r requirements-notebook.txt
#
# Fixes: AttributeError: module 'jinja2' has no attribute 'Markup'
# (Jinja2 3.1+ moved Markup; older nbconvert expected jinja2.Markup.)
nbconvert>=7.14
